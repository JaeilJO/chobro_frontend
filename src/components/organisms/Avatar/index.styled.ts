import { styled } from 'styled-components';

const S = {
    Avatar: styled.div`
        position: relative;
        display: flex;
        flex-direction: column;
        width: fit-content;
        align-items: end;
        position: relative;
    `,
};

export default S;
