import type { NextPage } from 'next';
import Logo from '../components/atoms/Logo';

const Home: NextPage = () => {
    return (
        <div>
            <Logo text="Hello" />
        </div>
    );
};

export default Home;
